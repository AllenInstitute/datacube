1. Change URL in load_test.py to point at desired crossbar router
2. Copy the contents of this directory to a location on the SAN accessible to the cluster
3. cd to the directory and run `./submit.sh N` where N is the number of nodes to use
